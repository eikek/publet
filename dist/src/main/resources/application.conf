publet {
  log-config = false
  server {
    bindAddress = "localhost"
    port = 8088
  }
}

akka {
  event-handlers = ["akka.event.slf4j.Slf4jEventHandler"]
  loglevel = "DEBUG"
}

# TODO switch to chunked api
spray.can.server {
  request-chunk-aggregation-limit = 100m
}
